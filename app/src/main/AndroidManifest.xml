<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.basic">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Basic">
        <activity android:name=".ViewFaculty" android:parentActivityName=".FacultyPanel"/>
        <activity android:name=".AddFaculty" android:parentActivityName=".FacultyPanel"/>
        <activity android:name=".ViewAttendance" android:parentActivityName=".FacultyPanel"/>
        <activity android:name=".ViewStudents" android:parentActivityName=".FacultyPanel" />
        <activity android:name=".FacultyPanel" />
        <activity
            android:name=".Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DayHistory" />
        <activity android:name=".RecordAttendance" />
        <activity android:name=".room.addNewStudent" android:parentActivityName=".FacultyPanel"/>
        <activity
            android:name=".TakeAttendance"
            android:label="@string/title_activity_take_attendance"
            android:theme="@style/Theme.Basic.NoActionBar" />
        <activity android:name=".NewUnit" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>